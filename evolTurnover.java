

public class evolTurnover {

	// holycrap I need only alteration
	// old pop in -> new pop out
		// selector and alteration should not have been specified
	
	// or not? not
	// gotta group selectors with alterations
	// have a class which includes both, returns children?
		// yes, good.
		// name please...
	
	//evolSelector   selector;
	//evolAlteration alteration;
	
	public evolTurnover() // evolSelector s, evolAlteration a
	{
		//selector	= s;
		//alteration	= a;
	}
	
	public evolPopulus apply(evolPopulus p)
	{
		//return alteration.apply(selector.select(p));
		return p;
	}
	
}
